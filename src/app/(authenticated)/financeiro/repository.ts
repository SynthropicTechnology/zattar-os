/**
 * Repository Layer — Re-export centralizado (padrão FSD)
 *
 * Este arquivo serve como ponto de entrada único para a camada de repositórios.
 * A implementação real está em `repository/`.
 *
 * @example
 * import { LancamentosRepository } from '@/app/(authenticated)/financeiro/repository';
 */
export * from './repository/index';
