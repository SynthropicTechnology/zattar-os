/**
 * Service Layer — Re-export centralizado (padrão FSD)
 *
 * Este arquivo serve como ponto de entrada único para a camada de serviços.
 * A implementação real está em `services/`.
 *
 * @example
 * import { LancamentosService } from '@/app/(authenticated)/financeiro/service';
 */
export * from './services/index';
