---
phase: 02-header-messages-media
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(authenticated)/chat/components/chat-header.tsx
  - src/app/(authenticated)/chat/components/message-status-icon.tsx
autonomous: true
requirements: [HEAD-01, HEAD-02, HEAD-03, HEAD-04]

must_haves:
  truths:
    - "Chat header renders with backdrop-blur glassmorphism background instead of bg-card"
    - "Header avatar is 36px rounded-xl with online indicator dot"
    - "Action buttons are 32px ghost-styled with muted icon color and hover bg-foreground/[0.04]"
    - "Mobile back button renders on small screens, video/phone buttons only on lg+"
  artifacts:
    - path: "src/app/(authenticated)/chat/components/chat-header.tsx"
      provides: "Glassmorphic chat header with avatar, status, action buttons"
      contains: "backdrop-blur"
    - path: "src/app/(authenticated)/chat/components/message-status-icon.tsx"
      provides: "12px status icons (reduced from 16px)"
      contains: "size-3"
  key_links:
    - from: "src/app/(authenticated)/chat/components/chat-header.tsx"
      to: "../domain"
      via: "ChatItem type import"
      pattern: "import.*ChatItem.*from.*domain"
    - from: "src/app/(authenticated)/chat/components/chat-header.tsx"
      to: "@/components/ui/avatar"
      via: "Avatar with AvatarIndicator"
      pattern: "AvatarIndicator"
---

<objective>
Refactor ChatHeader to glassmorphic Glass Briefing design and resize MessageStatusIcon to 12px.

Purpose: Deliver the transparent, blurred header that establishes Phase 2's visual identity for the chat conversation area. Also normalize status icon size for the smaller bubble timestamps used in Plan 02.
Output: Refactored chat-header.tsx with backdrop-blur glassmorphism, 36px rounded-xl avatar, ghost action buttons. Refactored message-status-icon.tsx with 12px icons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-header-messages-media/02-CONTEXT.md
@.planning/phases/02-header-messages-media/02-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/(authenticated)/chat/domain.ts:
```typescript
export interface ChatItem extends SalaChat {
  name?: string;
  image?: string;
  lastMessage?: string;
  date?: string;
  unreadCount?: number;
  usuario?: UsuarioChat;
}

export interface UsuarioChat {
  id: number;
  nomeCompleto: string;
  nomeExibicao: string | null;
  avatar?: string;
  onlineStatus?: "online" | "away" | "offline";
  lastSeen?: string;
}

export type MessageStatus = "sending" | "sent" | "forwarded" | "read" | "failed";
```

From src/app/(authenticated)/chat/components/chat-header.tsx (current interface):
```typescript
interface ChatHeaderProps {
  sala: ChatItem;
  onVideoCall: () => void | Promise<void>;
  onAudioCall: () => void | Promise<void>;
  onScreenshare?: () => void | Promise<void>;
}
```

From src/components/ui/avatar.tsx:
```typescript
// Avatar, AvatarFallback, AvatarImage, AvatarIndicator
// AvatarIndicator accepts variant: "online" | "away" | "offline"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ChatHeader to glassmorphic design</name>
  <files>src/app/(authenticated)/chat/components/chat-header.tsx</files>
  <read_first>
    - src/app/(authenticated)/chat/components/chat-header.tsx
    - src/app/(authenticated)/chat/domain.ts
    - src/components/ui/avatar.tsx
    - .planning/phases/02-header-messages-media/02-UI-SPEC.md (Header Specification section)
    - src/app/globals.css (chat tokens)
  </read_first>
  <action>
Refactor ChatHeader in-place (per D-08). Keep the same ChatHeaderProps interface and all existing callbacks. Changes:

**Container (HEAD-01):**
- Replace `className="flex justify-between gap-4 px-4 py-2 border-b bg-card"` with:
  `className="relative z-10 flex items-center justify-between px-5 py-3 border-b border-white/[0.06] dark:border-white/[0.06]"` plus inline style `{ backgroundColor: 'rgba(22,18,34,0.8)', backdropFilter: 'blur(20px)', WebkitBackdropFilter: 'blur(20px)' }` for dark mode. For light mode support add a light-mode class: `light:bg-white/80 light:border-border`.
  Alternatively, use Tailwind classes: `bg-[rgba(22,18,34,0.8)] dark:bg-[rgba(22,18,34,0.8)] light:bg-white/80 backdrop-blur-[20px]`

**Avatar (HEAD-02, per D-11 and D-12):**
- Avatar wrapper: `className="size-9 rounded-xl overflow-hidden"` (36px, rounded-xl per D-12)
- Keep Avatar, AvatarImage, AvatarFallback, AvatarIndicator from `@/components/ui/avatar`
- Apply `className="size-9"` on Avatar component (per D-11, override without changing shared component)
- AvatarFallback: `className="bg-primary/10 text-primary text-xs font-semibold"` with `generateAvatarFallback(name)`
- Online indicator: 8px dot via AvatarIndicator with variant={onlineStatus}

**Info section:**
- Name: `className="text-[0.8125rem] font-semibold text-foreground leading-[1.2]"` (13px)
- Online status: 8px green dot inline + "Online" in `text-[0.625rem]` with `style={{ color: 'rgba(52,211,153,0.7)' }}`
- Offline status: `className="text-[0.625rem] text-muted-foreground/50"` showing "Visto por ultimo {date}" or "Offline"
- Group subtitle: `className="text-[0.625rem] text-muted-foreground/50"` showing "Sala Geral" or "Grupo"

**Action buttons (HEAD-03, HEAD-04):**
- All buttons: `size="icon"` with `className="size-8 text-muted-foreground/55 hover:bg-foreground/[0.04] hover:text-foreground transition-colors duration-200"` and `variant="ghost"`
- Icons inside: `className="size-4"` (16px)
- Mobile back button: `className="size-8 lg:hidden"` with `variant="ghost"` (no outline)
- Desktop Video/Phone: wrapper `className="hidden lg:flex lg:gap-2"`
- Remove Monitor/Screenshare button from the header (it is not in the spec; keep the prop but don't render it as a header button)
- Keep Ellipsis dropdown (ChatUserDropdown) as-is
- Tooltip content: "Chamada de Video", "Chamada de Audio", "Mais opcoes"
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - chat-header.tsx contains `backdrop-blur` or `backdropFilter`
    - chat-header.tsx contains `rgba(22,18,34,0.8)` (glassmorphic bg)
    - chat-header.tsx contains `size-9` (36px avatar)
    - chat-header.tsx contains `rounded-xl` on avatar element
    - chat-header.tsx contains `text-[0.8125rem]` (13px header name)
    - chat-header.tsx contains `text-[0.625rem]` (10px status text)
    - chat-header.tsx contains `size-8` on action buttons (32px)
    - chat-header.tsx contains `hover:bg-foreground/[0.04]` on action buttons
    - chat-header.tsx contains `text-muted-foreground/55` on action buttons
    - chat-header.tsx does NOT contain `bg-card` (old style removed)
    - chat-header.tsx does NOT contain `variant="secondary"` on action buttons
    - chat-header.tsx does NOT contain `variant="outline"` on mobile back button
    - ChatHeaderProps interface unchanged (sala, onVideoCall, onAudioCall, onScreenshare)
    - TypeScript compilation passes with no new errors
  </acceptance_criteria>
  <done>ChatHeader renders with backdrop-blur glassmorphism, 36px rounded-xl avatar with online indicator, 32px ghost action buttons with muted colors, proper mobile/desktop responsive behavior. Props interface unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Resize MessageStatusIcon to 12px</name>
  <files>src/app/(authenticated)/chat/components/message-status-icon.tsx</files>
  <read_first>
    - src/app/(authenticated)/chat/components/message-status-icon.tsx
    - .planning/phases/02-header-messages-media/02-UI-SPEC.md (Timestamps & Status section)
  </read_first>
  <action>
Update all icon size classes in MessageStatusIcon from `h-4 w-4` (16px) to `size-3` (12px).

Specific changes per status:
- `sending`: Loader2 `className="size-3 shrink-0 text-muted-foreground animate-spin"`
- `failed`: AlertCircle `className="size-3 shrink-0 text-destructive"`
- `read`: CheckCheck `className="size-3 shrink-0 text-success/60"` (was text-success, now text-success/60 per spec)
- `forwarded`: CheckCheck `className="size-3 shrink-0 text-muted-foreground"`
- `sent`: Check `className="size-3 shrink-0 text-primary/50"` (was text-muted-foreground, now text-primary/50 per spec)
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os && grep -c "size-3" src/app/\(authenticated\)/chat/components/message-status-icon.tsx</automated>
  </verify>
  <acceptance_criteria>
    - message-status-icon.tsx contains `size-3` exactly 5 times (one per status case)
    - message-status-icon.tsx does NOT contain `h-4 w-4`
    - message-status-icon.tsx contains `text-primary/50` for sent status
    - message-status-icon.tsx contains `text-success/60` for read status
  </acceptance_criteria>
  <done>All message status icons render at 12px with updated colors per UI spec: sent=primary/50, read=success/60, failed=destructive, forwarded=muted-foreground, sending=muted-foreground spinning.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors
2. `grep "backdrop-blur" src/app/(authenticated)/chat/components/chat-header.tsx` returns matches
3. `grep "size-3" src/app/(authenticated)/chat/components/message-status-icon.tsx` returns 5 matches
4. `grep "bg-card" src/app/(authenticated)/chat/components/chat-header.tsx` returns 0 matches (old style removed)
</verification>

<success_criteria>
- ChatHeader displays glassmorphic background with backdrop-blur-[20px] and rgba(22,18,34,0.8)
- Avatar is 36px rounded-xl with online indicator
- Action buttons are 32px ghost with muted-foreground/55 color
- Mobile shows back arrow, desktop shows video/phone/details
- MessageStatusIcon renders at 12px with correct per-status colors
- Zero TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-header-messages-media/02-01-SUMMARY.md`
</output>
