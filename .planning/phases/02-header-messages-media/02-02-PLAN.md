---
phase: 02-header-messages-media
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(authenticated)/chat/components/date-separator.tsx
  - src/app/(authenticated)/chat/components/message-group.tsx
  - src/app/(authenticated)/chat/components/chat-bubbles.tsx
  - src/app/(authenticated)/chat/components/chat-content.tsx
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03, MSG-04, MSG-05, MSG-06, MSG-07]

must_haves:
  truths:
    - "Messages from the same sender in sequence are visually grouped with one shared avatar"
    - "Avatar appears aligned to the last bubble of the group, invisible for sent messages"
    - "Sender name appears only on first bubble of group in group/geral chats"
    - "Sent bubbles have asymmetric corners (14px-4px-14px-14px) with primary bg and purple shadow"
    - "Received bubbles have asymmetric corners (4px-14px-14px-14px) with received bg and subtle border"
    - "Timestamps and status icons appear only on the last bubble of each group"
    - "Date separators with horizontal lines and centered label appear between different days"
  artifacts:
    - path: "src/app/(authenticated)/chat/components/date-separator.tsx"
      provides: "DateSeparator component with horizontal lines and centered date label"
      contains: "DateSeparator"
    - path: "src/app/(authenticated)/chat/components/message-group.tsx"
      provides: "MessageGroup component that renders a group of same-sender messages with shared avatar"
      contains: "MessageGroup"
    - path: "src/app/(authenticated)/chat/components/chat-bubbles.tsx"
      provides: "Refactored TextChatBubble with asymmetric corners"
      contains: "rounded-[0.25rem_0.875rem_0.875rem_0.875rem]"
    - path: "src/app/(authenticated)/chat/components/chat-content.tsx"
      provides: "ChatContent with grouping logic, DateSeparator, and MessageGroup rendering"
      contains: "DateSeparator"
  key_links:
    - from: "src/app/(authenticated)/chat/components/chat-content.tsx"
      to: "./message-group"
      via: "import MessageGroup"
      pattern: "import.*MessageGroup.*from.*message-group"
    - from: "src/app/(authenticated)/chat/components/chat-content.tsx"
      to: "./date-separator"
      via: "import DateSeparator"
      pattern: "import.*DateSeparator.*from.*date-separator"
    - from: "src/app/(authenticated)/chat/components/message-group.tsx"
      to: "./chat-bubbles"
      via: "import ChatBubble"
      pattern: "import.*ChatBubble.*from.*chat-bubbles"
---

<objective>
Create DateSeparator and MessageGroup components, refactor text bubbles to asymmetric corners, and wire grouping logic in ChatContent.

Purpose: Transform flat message list into a grouped, visually rich conversation with WhatsApp-style sender grouping, asymmetric bubble corners, and date separators between days.
Output: Two new components (date-separator.tsx, message-group.tsx), refactored chat-bubbles.tsx (text bubble only), and updated chat-content.tsx with grouping logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-header-messages-media/02-CONTEXT.md
@.planning/phases/02-header-messages-media/02-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/(authenticated)/chat/domain.ts:
```typescript
export interface MensagemComUsuario extends MensagemChat {
  id: number;
  salaId: number;
  usuarioId: number;
  conteudo: string;
  tipo: TipoMensagemChat;
  createdAt: string;
  status?: MessageStatus;
  ownMessage?: boolean;
  data?: ChatMessageData;
  usuario: UsuarioChat;
}

export interface UsuarioChat {
  id: number;
  nomeCompleto: string;
  nomeExibicao: string | null;
  avatar?: string;
  onlineStatus?: "online" | "away" | "offline";
}

export interface ChatItem extends SalaChat {
  tipo: TipoSalaChat; // 'geral' | 'documento' | 'privado' | 'grupo'
}

export enum TipoMensagemChat {
  Texto = "texto", Arquivo = "arquivo", Imagem = "imagem",
  Video = "video", Audio = "audio", Sistema = "sistema",
}
```

From src/app/(authenticated)/chat/components/chat-content.tsx (current):
```typescript
interface ChatContentProps {
  mensagens: MensagemComUsuario[];
  salaAtiva: ChatItem | null;
}
```

From src/app/(authenticated)/chat/components/chat-bubbles.tsx:
```typescript
// Exports: ChatBubble (switch on message.tipo)
// Internal: TextChatBubble, FileChatBubble, AudioChatBubble, ImageChatBubble, VideoChatBubble
```

From src/lib/utils.ts:
```typescript
export function generateAvatarFallback(name: string): string;
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DateSeparator and MessageGroup components</name>
  <files>
    src/app/(authenticated)/chat/components/date-separator.tsx,
    src/app/(authenticated)/chat/components/message-group.tsx
  </files>
  <read_first>
    - src/app/(authenticated)/chat/components/chat-bubbles.tsx
    - src/app/(authenticated)/chat/domain.ts
    - src/components/ui/avatar.tsx
    - .planning/phases/02-header-messages-media/02-UI-SPEC.md (Date Separator and Message Group Layout sections)
    - .planning/phases/02-header-messages-media/02-CONTEXT.md (decisions D-01 through D-04, D-09)
  </read_first>
  <action>
**DateSeparator (per D-09, new file):**

Create `date-separator.tsx` with:
```
Props: { date: string } // ISO date string
```

Layout: `flex items-center gap-4 my-4`
- Left line: `flex-1 h-px bg-foreground/[0.04]`
- Label: `text-[0.625rem] font-semibold uppercase tracking-[0.08em] text-muted-foreground/35 whitespace-nowrap`
- Right line: `flex-1 h-px bg-foreground/[0.04]`
- Add `role="separator"` and `aria-label` with the formatted date text on the container.

Date formatting logic (PT-BR):
- Today: "Hoje, DD de MMMM" (e.g., "Hoje, 09 de Abril")
- Same year: "DD de MMMM" (e.g., "15 de Marco")
- Different year: "DD de MMMM de YYYY" (e.g., "22 de Dezembro de 2025")
- Use a lookup array for month names in Portuguese: ["Janeiro", "Fevereiro", "Marco", "Abril", "Maio", "Junho", "Julho", "Agosto", "Setembro", "Outubro", "Novembro", "Dezembro"]
- Compare date using `toDateString()` for "today" check.

**MessageGroup (per D-09, new file):**

Create `message-group.tsx` with:
```typescript
interface MessageGroupProps {
  messages: MensagemComUsuario[];
  isOwn: boolean;
  isGroupChat: boolean; // true for tipo === 'grupo' || tipo === 'geral'
}
```

Layout (per D-01, D-02, D-03, D-04):
- Container: `flex gap-[0.625rem] max-w-[70%]` with `self-end flex-row-reverse` for own messages, `self-start` for received.
- Avatar column: 28px avatar (`size-7 rounded-lg`), aligned to bottom via `self-end`. Use Avatar/AvatarFallback/AvatarImage from `@/components/ui/avatar`. For own messages, add `invisible` class (reserves space but hidden). Show avatar of the LAST message's usuario (per D-01). Fallback: `bg-primary/10 text-primary text-[0.625rem] font-semibold` with initials from `generateAvatarFallback`.
- Messages column: `flex flex-col gap-1` (4px gap between bubbles).
- Sender name (per D-02): Only render on FIRST bubble of group, only if `isGroupChat === true`. Style: `text-[0.625rem] font-semibold text-primary opacity-60 mb-1 pl-[0.125rem]`. Use `messages[0].usuario.nomeExibicao || messages[0].usuario.nomeCompleto`.
- For each message in the group, render `<ChatBubble message={msg} isFirstInGroup={index === 0} isLastInGroup={index === messages.length - 1} />`.
- Timestamp (per D-04): Only on last bubble — pass `showTimestamp={true}` to last ChatBubble, `showTimestamp={false}` to others.
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - date-separator.tsx exists and exports DateSeparator
    - date-separator.tsx contains `role="separator"`
    - date-separator.tsx contains `text-[0.625rem]` and `tracking-[0.08em]`
    - date-separator.tsx contains `bg-foreground/[0.04]` (separator lines)
    - date-separator.tsx contains Portuguese month names array (at least "Janeiro" and "Dezembro")
    - message-group.tsx exists and exports MessageGroup
    - message-group.tsx contains `max-w-[70%]`
    - message-group.tsx contains `size-7` (28px avatar)
    - message-group.tsx contains `invisible` class for own-message avatar
    - message-group.tsx contains `text-primary opacity-60` (sender name style)
    - message-group.tsx contains `generateAvatarFallback`
    - TypeScript compilation passes with no new errors
  </acceptance_criteria>
  <done>DateSeparator renders horizontal lines with centered PT-BR date label. MessageGroup renders a cluster of same-sender messages with 28px avatar on last bubble, sender name on first bubble in group chats, and timestamp delegation.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor text bubbles to asymmetric corners and wire grouping in ChatContent</name>
  <files>
    src/app/(authenticated)/chat/components/chat-bubbles.tsx,
    src/app/(authenticated)/chat/components/chat-content.tsx
  </files>
  <read_first>
    - src/app/(authenticated)/chat/components/chat-bubbles.tsx
    - src/app/(authenticated)/chat/components/chat-content.tsx
    - src/app/(authenticated)/chat/components/date-separator.tsx (just created in Task 1)
    - src/app/(authenticated)/chat/components/message-group.tsx (just created in Task 1)
    - src/app/(authenticated)/chat/domain.ts
    - .planning/phases/02-header-messages-media/02-UI-SPEC.md (Asymmetric Bubble Corners, Bubble Padding, Timestamps sections)
    - .planning/phases/02-header-messages-media/02-CONTEXT.md (decisions D-01 through D-04, D-08)
  </read_first>
  <action>
**Refactor ChatBubble export signature (chat-bubbles.tsx):**

Update ChatBubble to accept new props:
```typescript
interface ChatBubbleProps {
  message: MensagemComUsuario;
  isFirstInGroup?: boolean;  // controls top corner radius
  isLastInGroup?: boolean;   // controls whether to show timestamp
  showTimestamp?: boolean;    // explicit override
}
```

Keep backward compatibility: if `isFirstInGroup`/`isLastInGroup` are undefined, default to `true` (single-message behavior).

**Refactor TextChatBubble (MSG-04, MSG-05):**

Replace the entire TextChatBubble styling:

- Remove outer wrapper `max-w-[80%] lg:max-w-[60%]` (width control now in MessageGroup).
- Bubble div:
  - Received: `bg-(--chat-bubble-received) border border-white/[0.05] dark:border-white/[0.05]`
  - Sent: `bg-primary text-white shadow-lg shadow-primary/20`
  - Padding: `px-4 py-2` (16px horizontal, 8px vertical)
  - Font: `text-[0.8125rem] leading-[1.5]` (13px body)
  - Word wrap: `style={{ overflowWrap: "anywhere" }}`
  - Asymmetric corners for received: `rounded-[0.25rem_0.875rem_0.875rem_0.875rem]` (default), `rounded-[0.875rem]` when `isFirstInGroup === true` (all 14px)
  - Asymmetric corners for sent: `rounded-[0.875rem_0.25rem_0.875rem_0.875rem]` (default), `rounded-[0.875rem]` when `isFirstInGroup === true`
- Remove the inline dropdown menu from inside TextChatBubble (the context menu trigger should be on group-hover of the message row, keep it but move it outside the bubble content div).
- Timestamp section (only when `isLastInGroup !== false`):
  - Container: `flex items-center gap-[0.25rem] mt-[0.25rem] px-[0.125rem]` with `justify-end` for sent.
  - Time: `text-[0.625rem] text-muted-foreground/35 tabular-nums font-mono` (10px, Geist Mono via font-mono)
  - Status icon: `MessageStatusIcon` (already 12px from Plan 01)

**Leave FileChatBubble, AudioChatBubble, ImageChatBubble, VideoChatBubble unchanged** (they will be refactored in Plan 03). They still need to work — just keep their current code. But add the `isFirstInGroup`/`isLastInGroup`/`showTimestamp` props to the ChatBubble switch and pass them through.

**Wire grouping logic in ChatContent (MSG-01, MSG-02, MSG-06, MSG-07):**

Replace the flat message rendering with grouping:

1. Import DateSeparator and MessageGroup.
2. Create a grouping function `groupMessages(mensagens: MensagemComUsuario[])` that returns an array of:
   ```typescript
   type GroupedItem =
     | { type: 'date-separator'; date: string }
     | { type: 'message-group'; messages: MensagemComUsuario[]; isOwn: boolean };
   ```
3. Grouping rules (per D-03):
   - Messages from the same `usuarioId` in sequence form a group.
   - If the time gap between consecutive messages from the same sender exceeds 5 minutes, break into a new group.
   - Insert a `date-separator` item when the date (day) changes between consecutive messages.
4. Determine `isGroupChat` from `salaAtiva.tipo === 'grupo' || salaAtiva.tipo === 'geral'`.
5. Render: iterate over GroupedItem[], rendering DateSeparator or MessageGroup accordingly.
6. Keep the messagesEndRef scroll-to-bottom behavior.
7. Keep the empty state (no salaAtiva) rendering unchanged.
8. Memoize the grouping with `useMemo` keyed on `mensagens`.
9. Update container classes: `px-4` to `px-4 lg:px-6` and `py-8` to `pt-8 pb-4` for the messages area. Add `bg-(--chat-thread-bg)` if not already present (it currently uses `bg-chat-thread` which maps to the same token).
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - chat-bubbles.tsx contains `rounded-[0.25rem_0.875rem_0.875rem_0.875rem]` (received asymmetric)
    - chat-bubbles.tsx contains `rounded-[0.875rem_0.25rem_0.875rem_0.875rem]` (sent asymmetric)
    - chat-bubbles.tsx contains `shadow-lg shadow-primary/20` (sent bubble shadow)
    - chat-bubbles.tsx contains `text-[0.8125rem]` (13px body)
    - chat-bubbles.tsx contains `text-[0.625rem]` and `font-mono` (10px timestamp)
    - chat-bubbles.tsx contains `isFirstInGroup` and `isLastInGroup` props
    - chat-content.tsx contains `import.*DateSeparator` 
    - chat-content.tsx contains `import.*MessageGroup`
    - chat-content.tsx contains `groupMessages` function or equivalent
    - chat-content.tsx contains `useMemo` (memoized grouping)
    - chat-content.tsx contains `5 * 60 * 1000` or `300000` or equivalent 5-minute gap check
    - TypeScript compilation passes with no new errors
  </acceptance_criteria>
  <done>Text bubbles render with asymmetric corners (4px/14px), purple shadow on sent, proper timestamp formatting. ChatContent groups messages by sender with 5-min gap breaking, inserts DateSeparator between days, and renders MessageGroup clusters with shared avatar.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors
2. `grep -r "DateSeparator" src/app/(authenticated)/chat/components/` shows imports in chat-content.tsx
3. `grep -r "MessageGroup" src/app/(authenticated)/chat/components/` shows imports in chat-content.tsx
4. `grep "rounded-\[0.25rem" src/app/(authenticated)/chat/components/chat-bubbles.tsx` returns match (asymmetric corners)
5. `grep "groupMessages\|useMemo" src/app/(authenticated)/chat/components/chat-content.tsx` returns matches
</verification>

<success_criteria>
- DateSeparator renders between different days with "Hoje, DD de MMMM" format in PT-BR
- Messages from same sender are grouped with one 28px avatar at bottom
- Sender name shows only on first bubble of group in group/geral chats
- Text bubbles have asymmetric corners (4px corner where avatar connects)
- Sent bubbles: primary bg, white text, purple shadow
- Received bubbles: chat-bubble-received bg, subtle white border
- Timestamps only on last bubble of each group
- Non-text media bubbles still render correctly (untouched styling, but accept new props)
- Zero TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-header-messages-media/02-02-SUMMARY.md`
</output>
