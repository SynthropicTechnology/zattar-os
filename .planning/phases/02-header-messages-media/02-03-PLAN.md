---
phase: 02-header-messages-media
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/app/(authenticated)/chat/components/chat-bubbles.tsx
autonomous: true
requirements: [MEDIA-01, MEDIA-02, MEDIA-03, MEDIA-04, MEDIA-05]

must_haves:
  truths:
    - "File attachments render with IconContainer, file name, file size, and glass download button"
    - "Audio messages show a circular play/pause button, decorative waveform bars, and duration label"
    - "Images render with rounded-xl corners and 8px padding inside the bubble"
    - "Videos render with rounded-xl corners and native controls inside a black container"
    - "All media bubbles respect the asymmetric corner system from Plan 02"
  artifacts:
    - path: "src/app/(authenticated)/chat/components/chat-bubbles.tsx"
      provides: "Refactored FileChatBubble, AudioChatBubble, ImageChatBubble, VideoChatBubble"
      contains: "IconContainer"
  key_links:
    - from: "src/app/(authenticated)/chat/components/chat-bubbles.tsx"
      to: "@/components/ui/icon-container"
      via: "import IconContainer for file type icon"
      pattern: "import.*IconContainer.*from.*icon-container"
---

<objective>
Refactor all media bubble components (File, Audio, Image, Video) to match the Glass Briefing UI spec with IconContainer, audio waveform visual, and consistent rounded corners.

Purpose: Complete the message bubble redesign by upgrading media attachments to the Glass Briefing design system. Audio gets a custom WhatsApp-style waveform visual. Files get IconContainer + glass download button. Images and videos get consistent rounded-xl treatment.
Output: Refactored FileChatBubble, AudioChatBubble, ImageChatBubble, VideoChatBubble in chat-bubbles.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-header-messages-media/02-CONTEXT.md
@.planning/phases/02-header-messages-media/02-UI-SPEC.md

@.planning/phases/02-header-messages-media/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/(authenticated)/chat/domain.ts:
```typescript
export interface MensagemComUsuario extends MensagemChat {
  conteudo: string;
  tipo: TipoMensagemChat;
  createdAt: string;
  status?: MessageStatus;
  ownMessage?: boolean;
  data?: ChatMessageData;
  usuario: UsuarioChat;
}

export interface ChatMessageData {
  fileName?: string;
  fileUrl?: string;
  mimeType?: string;
  size?: string;
  duration?: string;
}
```

From src/components/ui/icon-container.tsx:
```typescript
interface IconContainerProps extends React.HTMLAttributes<HTMLDivElement> {
  size?: IconContainerSize; // "xs" | "sm" | "md" | "lg"
}
// Sizes: xs=20px, sm=24px, md=32px, lg=40px
export { IconContainer, ICON_CONTAINER_SIZES };
```

ChatBubble props (from Plan 02 refactor):
```typescript
interface ChatBubbleProps {
  message: MensagemComUsuario;
  isFirstInGroup?: boolean;
  isLastInGroup?: boolean;
  showTimestamp?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor FileChatBubble with IconContainer and glass download</name>
  <files>src/app/(authenticated)/chat/components/chat-bubbles.tsx</files>
  <read_first>
    - src/app/(authenticated)/chat/components/chat-bubbles.tsx (current state after Plan 02)
    - src/components/ui/icon-container.tsx
    - .planning/phases/02-header-messages-media/02-UI-SPEC.md (File Attachment section MEDIA-01)
  </read_first>
  <action>
Import `IconContainer` from `@/components/ui/icon-container` at the top of chat-bubbles.tsx.

Rewrite FileChatBubble layout to match spec:

Container: `flex items-center gap-3 p-3 pr-4 rounded-xl min-w-[240px]`
- Received: `bg-foreground/[0.03] border border-foreground/[0.06]`
- Sent: `bg-primary/[0.08] border border-primary/[0.12]`

Icon: Use `<IconContainer size="md" className={cn("rounded-lg", isOwn ? "bg-primary/12 text-primary" : "bg-info/10 text-info")}>`
- Inside: `<FileIcon className="size-4" />` (16px icon inside 32px container)
- Note: IconContainer md=32px, but spec says 36px. Use `className="size-9 rounded-lg"` on a plain div instead if IconContainer md is too small. Check actual size and decide.

File info: `flex flex-col min-w-0 flex-1`
- Name: `text-[0.75rem] font-semibold text-foreground truncate` (12px)
- Size: `text-[0.625rem] text-muted-foreground/40 mt-0.5` (10px)

Download button: `size-7 rounded-md bg-foreground/[0.04] text-muted-foreground/50 hover:bg-foreground/[0.08] hover:text-foreground transition-colors`
- Icon: `<Download className="size-3.5" />` (14px)
- Wrap in `<a href={fileUrl} target="_blank" rel="noopener noreferrer" download>` or Button asChild with anchor.
- Add `aria-label="Baixar {fileName}"`.

Timestamp: use the same isLastInGroup/showTimestamp pattern from TextChatBubble (render timestamp row only when last in group).

Remove the old FileChatBubble implementation entirely. Keep the export name and the switch case in ChatBubble.
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os && grep -c "IconContainer" src/app/\(authenticated\)/chat/components/chat-bubbles.tsx</automated>
  </verify>
  <acceptance_criteria>
    - chat-bubbles.tsx contains `import.*IconContainer.*from.*icon-container`
    - chat-bubbles.tsx contains `bg-foreground/[0.03]` (received file bg)
    - chat-bubbles.tsx contains `bg-primary/[0.08]` (sent file bg)
    - chat-bubbles.tsx contains `text-[0.75rem] font-semibold` (file name)
    - chat-bubbles.tsx contains `text-[0.625rem] text-muted-foreground/40` (file size)
    - chat-bubbles.tsx contains `aria-label` with "Baixar" (download a11y)
    - chat-bubbles.tsx contains `min-w-[240px]` (file bubble min width)
  </acceptance_criteria>
  <done>FileChatBubble renders with IconContainer (or 36px icon container), file name truncated, file size label, and glass download button with hover state. Sent and received have different bg tints.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AudioChatBubble with waveform visual and refactor Image/Video bubbles</name>
  <files>src/app/(authenticated)/chat/components/chat-bubbles.tsx</files>
  <read_first>
    - src/app/(authenticated)/chat/components/chat-bubbles.tsx (current state after Task 1)
    - .planning/phases/02-header-messages-media/02-UI-SPEC.md (Audio Message, Image Message, Video Message sections)
    - .planning/phases/02-header-messages-media/02-CONTEXT.md (decisions D-05, D-06, D-07)
  </read_first>
  <action>
**AudioChatBubble (MEDIA-02, per D-05, D-06, D-07):**

Replace the native `<audio>` element with a custom waveform visual. Keep a hidden `<audio>` element for actual playback.

Layout: inherits bubble styling (asymmetric corners from parent) + `flex items-center gap-[0.625rem] min-w-[220px]`
Padding: `px-4 py-2` (matches text bubble padding, 8px/16px)

Play button (per D-07): 32px circle, positioned inside bubble, left of waveform.
- `className="size-8 rounded-full shrink-0 flex items-center justify-center transition-transform active:scale-95"`
- Received: `bg-primary/15 text-primary`
- Sent: `bg-white/20 text-white`
- Icon: `Play` (14px, filled) or `Pause` when playing. Import from lucide-react.
- `aria-label="Reproduzir audio"`
- onClick: toggle play/pause on the hidden audio element via useRef.

Waveform (per D-05): decorative static bars.
- Container: `flex-1 h-6 flex items-center gap-1`
- Generate ~30 bars with deterministic pseudo-random heights. Use the message ID as seed: `Math.sin(messageId * (index + 1)) * 0.5 + 0.5` scaled to 4-20px range.
- Each bar: `w-1 rounded-full` (4px wide) with height via inline style.
- Received bars: `bg-primary/30`
- Sent bars: `bg-white/35`
- Progress: track current time / duration. Bars up to the progress point get brighter color: received `bg-primary/60`, sent `bg-white/60`.

Duration (per D-06): `text-[0.625rem] tabular-nums opacity-60 shrink-0 font-mono`
- Show "currentTime / totalDuration" format (e.g., "0:23 / 1:45").
- Parse `message.data?.duration` for total. Use audio element's `currentTime` for progress.
- Use `useState` for isPlaying and currentTime. Use `useRef` for audio element.
- On timeupdate event, update currentTime. On ended event, reset.

Respect `prefers-reduced-motion`: skip waveform bar height animation when reduced motion is preferred.

Fallback: keep `<noscript>` or hidden `<audio>` with `Seu navegador nao suporta audio.` text.

**ImageChatBubble (MEDIA-03):**

Simplify:
- Bubble padding: `p-2` (8px all sides)
- Image container: `rounded-xl overflow-hidden max-w-[280px]`
- Image: `w-full h-auto block rounded-xl object-cover`
- Alt text: `message.data?.fileName || "Imagem anexada"` (per accessibility spec)
- Keep Next.js Image component with `unoptimized` for external URLs.

**VideoChatBubble (MEDIA-04):**

Simplify:
- Container: `rounded-xl overflow-hidden bg-black`
- Video: `max-w-full rounded-xl max-h-[300px]` with native `controls`, `preload="metadata"`
- Remove extra wrapper divs.

**System message placeholder (MEDIA-05):**

Add a case for `tipo === "sistema"` in the ChatBubble switch:
- Render: `<div className="text-[0.625rem] text-muted-foreground/40 text-center py-2">{message.conteudo}</div>`
- No bubble, no avatar, centered.

Timestamp handling for all media bubbles: use the same isLastInGroup pattern. Only show timestamp row when `isLastInGroup !== false`.
  </action>
  <verify>
    <automated>cd /Users/jordanmedeiros/Projetos/zattar-os && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - chat-bubbles.tsx contains `rounded-full` and `size-8` (play button 32px circle)
    - chat-bubbles.tsx contains `bg-primary/15` (received play button)
    - chat-bubbles.tsx contains `bg-white/20` (sent play button)
    - chat-bubbles.tsx contains `gap-1` and `h-6` (waveform container)
    - chat-bubbles.tsx contains `w-1 rounded-full` (waveform bars)
    - chat-bubbles.tsx contains `bg-primary/30` (received waveform bars)
    - chat-bubbles.tsx contains `bg-white/35` (sent waveform bars)
    - chat-bubbles.tsx contains `aria-label="Reproduzir audio"`
    - chat-bubbles.tsx contains `min-w-[220px]` (audio bubble min width)
    - chat-bubbles.tsx contains `max-w-[280px]` (image max width)
    - chat-bubbles.tsx contains `max-h-[300px]` (video max height)
    - chat-bubbles.tsx contains `"Imagem anexada"` (alt text fallback)
    - chat-bubbles.tsx contains `text-center py-2` (system message)
    - chat-bubbles.tsx contains `Math.sin` or similar for waveform height generation
    - TypeScript compilation passes with no new errors
  </acceptance_criteria>
  <done>AudioChatBubble renders with 32px play/pause circle, ~30 decorative waveform bars with progress tracking, and "currentTime / duration" label. ImageChatBubble renders with 8px padding and rounded-xl. VideoChatBubble renders with black bg and native controls. System messages render centered without bubble. All media bubbles respect asymmetric corner system and timestamp grouping.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors
2. `grep "IconContainer" src/app/(authenticated)/chat/components/chat-bubbles.tsx` returns match
3. `grep "Reproduzir audio" src/app/(authenticated)/chat/components/chat-bubbles.tsx` returns match
4. `grep "Imagem anexada" src/app/(authenticated)/chat/components/chat-bubbles.tsx` returns match
5. `grep "max-w-\[280px\]" src/app/(authenticated)/chat/components/chat-bubbles.tsx` returns match (image)
6. `grep "max-h-\[300px\]" src/app/(authenticated)/chat/components/chat-bubbles.tsx` returns match (video)
</verification>

<success_criteria>
- File bubble: IconContainer with type-specific colors, truncated name, size label, glass download button
- Audio bubble: Play/pause toggle, ~30 waveform bars with progress color change, "0:23 / 1:45" duration
- Image bubble: 8px padding, rounded-xl corners, max-w-280px, proper alt text
- Video bubble: Black bg, rounded-xl, native controls, max-h-300px
- System messages: Centered, no bubble, muted text
- All media bubbles integrate with MessageGroup avatar/timestamp system
- Zero TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-header-messages-media/02-03-SUMMARY.md`
</output>
